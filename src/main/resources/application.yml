spring:
  application:
    name: file_upload_service
  flyway:
    enabled: true
    locations: classpath:db/migration
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: always
server:
  port: 20001
logging:
  level:
    org.flywaydb: info
